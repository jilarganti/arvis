{
    "installCommand": "pnpm install -r --no-frozen-lockfile --strict-peer-dependencies",
    "buildCommand": "pnpm build",
    "outputDirectory": "packages/site/.vitepress/dist",
    "cleanUrls": true
}
