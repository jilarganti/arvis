packages:
    # include packages that are inside this directories
    - 'docs'
    - 'apps/*'
    - 'packages/*'

    # exclude packages that are inside this directories
    - '!apps/create-arvis'
    - '!packages/**/__tests__/**'
